Function merge() accepts as arguments an arbitrary number of iterables (lists, tuples),
each of which generates sorted numbers. \
Function merges the outputs of the iterables without overloading a memory.